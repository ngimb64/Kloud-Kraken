- Look into pricing system retrieving wrong prices
    - Try adding more filters to ensure correct prices are retrieved

- Optimize awscost and add cost prediction functionality

- Look into making sure VPC flow logs are properly working and optimized

- Try improving existing tests related to file io with potential memory-based io providers (read blog)
    - Also look into using testing lib for file related operations instead of os

- Implement unit tests for packages that need them

- Look into using custom generated key for KMS encryption utilized with S3, SSM, and CloudWatch logging
    - Ensure to update permissions policies as needed

- Optimize wordlist merging and file shaving logic with custom rust programs

- Look into removing sprintf usage in kloudlogs or replace it with simpler static logging functions

- Look into using packer to create custom AMI tailored to the project with saved driver install code
    - When implemented adjust code to use kloud-kraken AMI if it is available other wise fall back to deep learning base AMI
