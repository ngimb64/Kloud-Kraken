- Implement teardown program that ensures all resources in state file are terminated
    X- S3 contents & bucket
        - Empty bucket: ListObjectsV2 + DeleteObjects (multipart / versioned buckets require special handling).
        - Delete bucket policy if you want: DeleteBucketPolicy.
        - DeleteBucket.

    X- SSM Parameter Store entries
        - DeleteParameter for TLS certs.

    - CloudWatch / Flow Logs
        - Delete CloudWatch log groups used by clients & flow logs: DeleteLogGroup.
        - Delete flow logs: DeleteFlowLogs.

    X- VPC endpoints
        - DeleteVpcEndpoints for S3, SSM, etc.

    X- NAT Gateways & EIP
        - DeleteNatGateway (wait for it to be removed/available).
        - After NAT is deleted and the EIP is disassociated, ReleaseAddress for the Elastic IP(s).

    X- Route tables & associations
        - Disassociate route table associations: DisassociateRouteTable (or modify associations).
        - Remove custom routes (DeleteRoute) if necessary.
        - DeleteRouteTable.

    X- Internet Gateway
        - DetachInternetGateway from VPC.
        - DeleteInternetGateway.

    X- Subnets
        - DeleteSubnet for each public and private subnet.

    X- Security groups
        - Revoke any rules: RevokeSecurityGroupIngress / RevokeSecurityGroupEgress.
        - DeleteSecurityGroup.

    X- VPC
        - Ensure nothing remains (endpoints, ENIs, NATs, subnets, IGW).
        - DeleteVpc.

    - IAM cleanup
        - For each instance profile:
            - Remove roles from instance profiles: RemoveRoleFromInstanceProfile.
            - Delete instance profiles: DeleteInstanceProfile.
        - For each role (flow-logs role, client role, server role, etc.):
            - Delete inline policies: DeleteRolePolicy.
            - Detach managed policies: DetachRolePolicy.
            - Delete any managed policies created: DeletePolicy.
            - Delete roles: DeleteRole.

    - Make sure permissions list and README is updated

- Add logic to remove security group rules on client after updates are complete

- Make sure all policies are updated based on review
    - Update README with bootstrap policy

- Mess around with GPU instance manually ensuring instance drive raid setup process in user data script is correct

- Run system testing in AWS environment

- Implement system of calculating cost of resources
    - Ensure to update permissions policies as needed

- Look into using custom generated key for KMS encryption utilized with S3 and SSM
    - Ensure to update permissions policies as needed

- Re-run system testing in AWS environment

- Finish documentation for AWS code

- Implement unit tests for packages that need them

- Try improving tests related to file io with potential memory-based io providers (read blog)
    - Also look into using testing lib for file related operations instead of os

- Optimize wordlist merging and file shaving logic with custom rust programs





NOTE:  below list is reference for research item in list

Server Bootstrap role:
    - Create VPC with CIDR block
        - CreateTags
        - CreateVpc
        - DescribeVpcs
        - NewVpcExistsWaiter
    - Create and attach Internet Gateway (IGW) to the VPC
        - AttachInternetGateway
        - CreateInternetGateway
        - CreateTags
        - DescribeInternetGateways
        - NewInternetGatewayExistsWaiter
    - Allocate Elastic IP for NAT Gateway
        - AllocateAddress
        - CreateTags
        - DescribeAddresses
    - Get the availability zones for the region
        - DescribeAvailabilityZones
    - Create public and private subnets within the VPC
        - CreateSubnet
        - CreateTags
        - DescribeSubnets
        - ModifySubnetAttribute
        - NewSubnetAvailableWaiter
    - Create NAT Gateway in public subnet (uses Elastic IP)
        - CreateNatGateway
        - CreateTags
        - DescribeNatGateways
        - NewNatGatewayAvailableWaiter
    - Create route table for public subnets: 0.0.0.0/0 → IGW
        - AssociateRouteTable
        - CreateRouteTable
        - CreateRoute
        - CreateTags
        - DescribeRouteTables
    - Create route tables for private subnets (per AZ): 0.0.0.0/0 → NAT Gateway
        - Same as above
    - Associate **public** subnets to public route table
        - AssociateRouteTable
        - DescribeRouteTables
    - Associate **private** subnets to private route tables
        - Same as above
    - Create security groups for EC2
        - CreateSecurityGroup
        - CreateTags
        - DescribeSecurityGroups
        - NewSecurityGroupExistsWaiter
        - RevokeSecurityGroupEgress
    - Add rules to EC2 security group to allow updates and package installation
        - AuthorizeSecurityGroupEgress
        - DescribeSecurityGroups
    - Create security group for SSM Parameter Store
        - Same as above
    - Add rules to SSM parameter store for allowing storing and grabbing TLS certs
        - AuthorizeSecurityGroupIngress
        - DescribeSecurityGroups
    - Create S3 bucket & add bucket policy restricting access to VPC/VPC endpoint
        - CreateBucket
        - HeadBucket
        - NewBucketExistsWaiter
    - Create VPC endpoints (e.g., S3, SSM) for private access
        - DescribeVpcEndpoints
        - CreateVpcEndpoint
        - CreateTags
    - Create IAM role and trust policy for VPC flow logs
        - CreateRole
        - CreateTags
        - GetRole
        - PutRolePolicy
    - Enable VPC Flow Logs for traffic monitoring and auditing
        - CreateFlowLogs
        - CreateLogGroup
        - CreateTags
        - DescribeFlowLogs
        - PutRetentionPolicy
    - Create IAM role for client
        - AddRoleToInstanceProfile
        - CreateInstanceProfile
        - CreateRole
        - CreateTags
        - GetRole
        - PutRolePolicy
    - Create IAM role for server
        - CreateRole
        - CreateTags
        - GetRole
        - PutRolePolicy
    - Assume Server role after bootstrap is complete
        - NewAssumeRoleProvider

Server role:
    - Put the generated TLS certificate SSM parameter store
        - PutParameter
        - CreateTags
    - Put client binary into S3 bucket
        - PutObject
    - Setup EC2 instances
        - CreateTags
        - NewInstanceExistsWaiter
        - RunInstances
    - Terminate NAT Gateway
        - DeleteNatGateway
    - Terminate EC2 instances
        - TerminateInstances
    - Terminate Elastic IP
        - ReleaseAddress
    - Terminate SSM VPC Endpoint
        - DeleteVpcEndpoints
    - Terminate S3 bucket and its contents
        - ListObjectsV2
        - DeleteObjects
        - DeleteBucket
    - Terminate TLS certificate from SSM param store
        - DeleteParameter

Client role:
    - Get the client binary from s3 in user data script using aws s3 cp command in user data script
        - GetObject
    - Get the TLS certificate from SSM parameter store
        - GetParameter
    - Logging system with CloudWatch integration
        - CreateLogGroup
        - CreateLogStream
        - CreateTags
        - DescribeLogStreams
        - GetMetadata
        - PutLogEvents
        - PutRetentionPolicy
    - Create security group rule to open port for file transfer
        - AuthorizeSecurityGroupEgress
        - DescribeSecurityGroups
    - Revoke security group rule to close port after file transfer
        - RevokeSecurityGroupEgress
